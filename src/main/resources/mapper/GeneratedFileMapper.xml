<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaee.docmanager.ai.mapper.GeneratedFileMapper">

    <insert id="insert" parameterType="com.javaee.docmanager.ai.entity.GeneratedFile">
        INSERT INTO generated_file (id, title, file_format, file_id, object_key, current_version_id, create_by, create_time)
        VALUES (#{id}, #{title}, #{fileFormat}, #{fileId}, #{objectKey}, #{currentVersionId}, #{createBy}, #{createTime})
    </insert>

    <select id="selectById" resultType="com.javaee.docmanager.ai.entity.GeneratedFile">
        SELECT * FROM generated_file WHERE id = #{id}
    </select>

    <select id="selectRecent" resultType="com.javaee.docmanager.ai.entity.GeneratedFile">
        SELECT * FROM generated_file
        <if test="createBy != null and createBy != ''">
            WHERE create_by = #{createBy}
        </if>
        ORDER BY create_time DESC LIMIT #{limit}
    </select>

    <delete id="deleteById">
        DELETE FROM generated_file WHERE id = #{id}
    </delete>

    <update id="updateTitle">
        UPDATE generated_file SET title = #{title} WHERE id = #{id}
    </update>

    <update id="updateCurrentVersion">
        UPDATE generated_file
        SET current_version_id = #{currentVersionId},
            title = #{title},
            object_key = #{objectKey},
            file_id = #{objectKey}
        WHERE id = #{id}
    </update>

</mapper>
