<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaee.docmanager.ai.aiops.ErrorLogMapper">

    <select id="selectLatest" resultType="com.javaee.docmanager.ai.aiops.ErrorLog">
        SELECT * FROM error_log
        ORDER BY update_time DESC, create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectBySourceAndMessage" resultType="com.javaee.docmanager.ai.aiops.ErrorLog">
        SELECT * FROM error_log
        WHERE source = #{source} AND error_message = #{errorMessage}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.javaee.docmanager.ai.aiops.ErrorLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO error_log (source, error_message, error_count, ai_analysis, create_time, update_time)
        VALUES (#{source}, #{errorMessage}, #{errorCount}, #{aiAnalysis}, NOW(), NOW())
    </insert>

    <update id="incrementCount">
        UPDATE error_log
        SET error_count = error_count + 1,
            update_time = NOW()
            <if test="aiAnalysis != null and aiAnalysis != ''">
                , ai_analysis = #{aiAnalysis}
            </if>
        WHERE id = #{id}
    </update>

</mapper>
